"""
RetailSense AI — API v1 sub-package

All versioned route modules for /api/v1/* live here.  Each feature area
(auth, products, inventory, …) has its own module; they are imported and
registered in ``app.api.router``.
"""
