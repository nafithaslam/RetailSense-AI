# =============================================================================
# RetailSense AI — Backend Dependencies
# =============================================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- Database ---
sqlalchemy==2.0.30
asyncpg==0.29.0           # Async PostgreSQL driver
alembic==1.13.1           # Database migrations

# --- Data Validation ---
pydantic==2.7.1
pydantic-settings==2.3.0

# --- Environment & Configuration ---
python-dotenv==1.0.1

# --- HTTP Client (for health checks, external APIs) ---
httpx==0.27.0

# --- Logging ---
structlog==24.2.0

# --- Security ---
# pwdlib replaces the unmaintained passlib[bcrypt].
# passlib 1.7.4 is broken against bcrypt >=4.1 (AttributeError: __about__).
# pwdlib 0.3.0 supports both Argon2 (primary, OWASP recommended) and
# Bcrypt (secondary, for any future legacy hash migration).
pwdlib[argon2,bcrypt]==0.3.0
argon2-cffi==25.1.0           # Argon2 C-binding (pwdlib[argon2] dep)
argon2-cffi-bindings==25.1.0  # Low-level Argon2 C-extension
python-jose[cryptography]==3.3.0  # JWT creation & verification (HS256 / RS256)
python-multipart==0.0.9       # Required by FastAPI for OAuth2 form-based login

# --- Dev / Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
